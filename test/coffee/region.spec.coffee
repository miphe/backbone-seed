
expect = require('chai').expect
Mn = require 'backbone.marionette'

# regions = require '../../app/coffee/utils/region.coffee'
#
# module.exports = describe 'Region module', ->
#
#   it 'should exist', ->
#     expect(regions).to.exist
#
#   it 'should be an object', ->
#     expect(regions).to.be.an 'object'
#
#   it 'should have some properties', ->
#     expect(regions).to.have.property 'Content'
#     expect(regions).to.have.property 'Statics'
#
#   describe 'Instance', ->
#
#     it 'should be an instance of RegionManager', ->
#       expect(regions.Content).to.be.instanceof Mn.RegionManager
#       expect(regions.Statics).to.be.instanceof Mn.RegionManager
